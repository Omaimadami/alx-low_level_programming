#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	int i = ('a');

	while (i < 'z' i != 'q' i != 'e')
	{
		putchar(i);
		i++;
	}
	putchar('\n');
	return (0);
}
